import { PowerBiReportDetails } from '../models/embed-report-config';

describe('PowerBiReportDetails', () => {
  it('should be defined', () => {
    expect(new PowerBiReportDetails()).toBeDefined();
  });
});
